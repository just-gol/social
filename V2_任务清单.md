# Social V2 任务清单

## 1. 文档目标

本文档用于记录 `social` 项目 V2 的实际研发完成情况，而不是继续保留旧的规划状态。

状态定义：

- `Done`：已经实现并验证
- `Partial`：已完成主体，但仍有增强空间
- `Deferred`：明确延后到后续版本

---

## 2. V2 总体结论

### 当前结论

- V2 合约核心：`Done`
- V2 测试补齐：`Done`
- V2 前端基础社交产品化：`Done`
- V2 增强产品化项：`Deferred`

### 当前 V2 的实际范围

本轮 V2 最终落地的是：

- 评论
- 关注 / 取关
- 评论 / 关注事件
- 评论 / 关注统计
- 前端社交列表
- 用户详情弹层
- 基础通知页
- Feed 过滤

---

## 3. 合约任务状态

## 3.1 Comment 数据模型与指令

状态：`Done`

已完成：

- [x] 新增 `Comment` 状态结构体
- [x] 定义 `Comment` PDA 规则
- [x] 新增 `create_comment`
- [x] 新增 `delete_comment`
- [x] 新增评论事件

已落文件：

- `/home/lsy/social/programs/social/src/state/comment.rs`
- `/home/lsy/social/programs/social/src/instructions/comment.rs`
- `/home/lsy/social/programs/social/src/events.rs`
- `/home/lsy/social/programs/social/src/lib.rs`

当前行为说明：

- 一人一帖一条评论
- 评论是一级评论
- 删除采用软删除
- Tweet 删除后不可新增评论
- Tweet 删除后评论作者仍可删除自己评论

---

## 3.2 Follow 数据模型与指令

状态：`Done`

已完成：

- [x] 新增 `Follow` 状态结构体
- [x] 定义 `Follow` PDA 规则
- [x] 新增 `create_follow`
- [x] 新增 `cancel_follow`
- [x] 新增关注事件

已落文件：

- `/home/lsy/social/programs/social/src/state/follow.rs`
- `/home/lsy/social/programs/social/src/instructions/follow.rs`
- `/home/lsy/social/programs/social/src/events.rs`
- `/home/lsy/social/programs/social/src/lib.rs`

当前行为说明：

- 禁止关注自己
- 禁止重复关注
- 取消关注时关闭 Follow 账户
- Follow 账户同时保存：
  - `follower`
  - `following`
  - `follower_profile_pda`
  - `following_profile_pda`

---

## 3.3 Profile 统计扩展

状态：`Done`

已完成：

- [x] 增加 `authority`
- [x] 增加 `comments_received_count`
- [x] 增加 `followers_count`
- [x] 增加 `following_count`
- [x] 增加对应增减方法

已落文件：

- `/home/lsy/social/programs/social/src/state/profile.rs`

当前语义：

- `comments_received_count`：该用户所有 Tweet 收到的评论总数
- `followers_count`：该用户的粉丝数
- `following_count`：该用户关注的人数

---

## 3.4 Tweet 评论统计

状态：`Done`

已完成：

- [x] 增加 `comments_count`
- [x] 评论创建时增加计数
- [x] 评论删除时回退计数

已落文件：

- `/home/lsy/social/programs/social/src/state/tweet.rs`
- `/home/lsy/social/programs/social/src/instructions/comment.rs`

---

## 4. 测试任务状态

## 4.1 Comment 测试

状态：`Done`

已完成：

- [x] 创建评论成功
- [x] 删除自己的评论成功
- [x] 已删除 Tweet 不能评论
- [x] 不能删除别人的评论
- [x] 重复评论失败
- [x] Tweet 删除后仍可删除自己的评论
- [x] 评论二次删除失败

已落文件：

- `/home/lsy/social/tests/comment.spec.ts`

实际验证结果：

- `7 passing`

---

## 4.2 Follow 测试

状态：`Done`

已完成：

- [x] 创建关注成功
- [x] 取消关注成功
- [x] 自己关注自己失败
- [x] 重复关注失败
- [x] 非本人不能取消别人的关注关系
- [x] 校验 `follower / following` 字段断言

已落文件：

- `/home/lsy/social/tests/follow.spec.ts`

实际验证结果：

- `5 passing`

---

## 4.3 既有测试同步

状态：`Done`

已完成：

- [x] 同步 `Profile` 新字段断言
- [x] 同步 `Tweet.comments_count` 断言

涉及文件：

- `/home/lsy/social/tests/profile.spec.ts`
- `/home/lsy/social/tests/tweet.spec.ts`

---

## 5. 前端任务状态

## 5.1 Comment / Follow 基础交互

状态：`Done`

已完成：

- [x] 创建评论
- [x] 删除自己的评论
- [x] Follow
- [x] Unfollow
- [x] 在 Tweet 卡片内看到评论和关注状态

已落文件：

- `/home/lsy/social/app/web/src/App.tsx`
- `/home/lsy/social/app/web/src/lib/socialClient.ts`
- `/home/lsy/social/app/web/src/lib/pda.ts`

---

## 5.2 Followers / Following 列表

状态：`Done`

已完成：

- [x] 打开 followers 列表
- [x] 打开 following 列表
- [x] 列表中显示用户基本资料
- [x] 列表中直接执行 follow / unfollow

---

## 5.3 用户详情视图

状态：`Done`

已完成：

- [x] 点击 Tweet 作者进入用户详情
- [x] 点击评论作者进入用户详情
- [x] 点击 followers / following 列表项进入用户详情
- [x] 用户详情显示：
  - 名称
  - Bio
  - 地址
  - Tweet 数
  - Followers 数
  - Following 数
  - Comments Received 数
  - 最近 tweets
  - Follow / Unfollow

说明：

- 当前采用弹层详情，不是独立路由页

---

## 5.4 Feed 产品化补充

状态：`Done`

已完成：

- [x] `Latest` 过滤
- [x] `Following` 过滤
- [x] `Mine` 过滤
- [x] 已删除 Tweet 前端不展示

说明：

- 目前仍是链上账户直读型 feed
- 未引入链下索引

---

## 5.5 通知页

状态：`Done`

已完成：

- [x] 新增 `Notifications` tab
- [x] 展示新关注通知
- [x] 展示别人评论你 Tweet 的通知
- [x] 展示作者待结算奖励提醒
- [x] 通知项可以跳到用户详情弹层

说明：

- 当前为前端聚合型通知
- 不做已读 / 未读持久化

---

## 5.6 基础前端体验修复

状态：`Done`

已完成：

- [x] tab 切换刷新逻辑优化
- [x] localnet / local keypair 模式稳定化
- [x] 关注弹层 `Unfollow` 文案统一为英文
- [x] 头像和社交卡片可点击进入详情
- [x] 前端错误提示继续沿用 V1 的人话翻译逻辑

---

## 6. 部署与执行链

## 6.1 测试脚本

状态：`Done`

已完成：

- [x] `Anchor.toml` 的 test script 从 `yarn` 改成 `npx ts-mocha`

当前脚本：

- `/home/lsy/social/Anchor.toml`

效果：

- 后续执行 `anchor test` 不再依赖本机安装 `yarn`

---

## 6.2 本地验证结果

状态：`Done`

已完成验证：

- [x] `anchor build`
- [x] `comment.spec.ts`
- [x] `follow.spec.ts`
- [x] `app/web` 构建

实际通过命令：

```bash
cd /home/lsy/social
anchor build

ANCHOR_PROVIDER_URL=http://127.0.0.1:8899 \
ANCHOR_WALLET=/home/lsy/.config/solana/id.json \
npx ts-mocha -p ./tsconfig.json -t 1000000 tests/comment.spec.ts

ANCHOR_PROVIDER_URL=http://127.0.0.1:8899 \
ANCHOR_WALLET=/home/lsy/.config/solana/id.json \
npx ts-mocha -p ./tsconfig.json -t 1000000 tests/follow.spec.ts

cd /home/lsy/social/app/web
npm run build
```

---

## 7. Deferred 项目

以下内容不再算当前 V2 阻塞项：

### 7.1 独立用户主页路由

状态：`Deferred`

原因：

- 当前弹层详情已经足够支撑 V2 的基础社交浏览
- 独立路由更适合放入下一版产品化重构

### 7.2 链下索引 / 查询服务

状态：`Deferred`

原因：

- 当前链上直读 + 前端聚合已满足 V2
- 若继续扩展 feed、通知、搜索，再引入索引层更合适

### 7.3 奖励记录页

状态：`Deferred`

原因：

- 当前奖励信息已可在 Tweet、通知、Vault 中看到基础状态
- 独立 Rewards 页面可以放入下一版

### 7.4 通知已读 / 未读

状态：`Deferred`

原因：

- 当前通知仍为实时聚合型视图
- 未做持久化读取状态

### 7.5 分页 / 搜索 / 推荐

状态：`Deferred`

原因：

- 不属于当前 V2 核心目标

---

## 8. 最终结论

按当前实际交付结果，V2 任务状态应定义为：

- 合约：`Done`
- 测试：`Done`
- 前端核心社交交互：`Done`
- 前端产品化增强：`Done（按当前 V2 范围）`
- 更高级产品化项：`Deferred`

因此当前代码库可以认为：

**V2 已完成，后续如果继续推进，应进入 V3 或 V2.1 增强阶段。**
